new_offer:
  title: "Offer {{ obj.title }} is now live!"
  body: "{{ obj.body }}"
  push_data: '{"end": "{{ obj.end }}"}'
  image_url: "{{ obj.image_url }}"
  
offer_reminder:
  title: "Offer {{ obj.title }} is ending soon!"
  body: "{{ obj.body }}"
  push_data: '{"end": "{{ obj.end }}"}'
  image_url: "{{ obj.image_url }}"